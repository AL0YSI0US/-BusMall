'use strict';

// stretchy things

// Create a div element with text: 'votesRemainingText'  Votes Remaining:
let votesRemainingText = document.querySelector('votesRemainingText');


// create a div element with one letter to position on page and then erase the letter.
// after each click count down and display how many votes the user has left.
let votesRemainingNumber = document.querySelector('votesRemainingNumber');

// place in Product constructor
this.votesToGo = 0;

// Creat an incrementer for the handleClick function?
if (i=0 < .votesRemainingNumber --;
// Perhaps place in handleclick too
countingDown();


function countingDown() {
  if (productClicked === votesAuthorized) {
    let li = document.getElementById('votesRemainingNumber');
    li.textContent = votesToGo   
}

//               finish figuring this bizzzzzzzzzzz out ya goob!

// also: my keeping naming convention for funtionas product i can add another mall in theory/ create an empty Array, create variables to sort things out, mayby push stuff where it needs to be? 
// if === go xyz?